server {
	listen 80;
	listen [::]:80;

	root /home/patricia/www/dialnerd.me;

	index index.html;

	server_name dialnerd.me www.dialnerd.me;

	location / {
		#first attempt to serve request as file, then
		# as directory, then fall back to displaying a 404.
		try_files $uri $uri/ =404;
	}
}
